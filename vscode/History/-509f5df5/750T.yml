networks:
  proxy:
    external: true
  appnet:
    external: true

services:
  portfolio-next:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: portfolio-next

    env_file:
      - ./.env
    environment:
      - NODE_ENV=production
      - PORT=3001
      - NEXT_TELEMETRY_DISABLED=1

    expose:
      - '3001'

    restart: unless-stopped

    networks:
      - proxy
      - appnet

    labels:
      - 'traefik.enable=true'
      - 'traefik.docker.network=proxy'

      # ---- Apex router ----
      - 'traefik.http.routers.ns.rule=Host(`tariqul.dev`)'
      - 'traefik.http.routers.ns.entrypoints=websecure'
      - 'traefik.http.routers.ns.tls=true'
      - 'traefik.http.routers.ns.tls.certresolver=letsencrypt'
      - 'traefik.http.routers.ns.service=ns'

      # ---- Service ----
      - 'traefik.http.services.ns.loadbalancer.server.port=3001'
      - 'traefik.http.services.ns.loadbalancer.passhostheader=true'

      # ---- www → non-www redirect (HTTPS) ----
      - 'traefik.http.routers.ns-www.rule=Host(`www.tariqul.dev`)'
      - 'traefik.http.routers.ns-www.entrypoints=websecure'
      - 'traefik.http.routers.ns-www.tls=true'
      - 'traefik.http.routers.ns-www.tls.certresolver=letsencrypt'
      - 'traefik.http.routers.ns-www.middlewares=redirect-to-apex@docker'

      # Redirect middleware (preserves path & query)
      - "traefik.http.middlewares.redirect-to-apex.redirectregex.regex=^https?://www\\.tariqul\\.dev(.*)"
      - 'traefik.http.middlewares.redirect-to-apex.redirectregex.replacement=https://tariqul.dev$1'
      - 'traefik.http.middlewares.redirect-to-apex.redirectregex.permanent=true'
